# Quickstart — 005-toolbox-r2

```bash
cd src/web
npm install
npm test
npm run build
npm run dev
```

Abrir `/` → Ferramentas → abas Transformar / Schema / Segurança / Mock / SQL.  
SEO: `/ferramentas/json-schema`, `/ferramentas/json-patch`, etc.
