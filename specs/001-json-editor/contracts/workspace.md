# Contracts — 001-json-editor

Não há contrato HTTP de documento.

Persistência: `localStorage['jsonmais.workspace.v1']` JSON:

```json
{ "version": 1, "left": "{...}", "right": "{...}" }
```

Download: `application/json` filename `documento.json`.
