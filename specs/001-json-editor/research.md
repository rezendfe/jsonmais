# Research — 001-json-editor

JSON Editor Online é o produto comercial em torno de svelte-jsoneditor / vanilla-jsoneditor (Jos de Jong). Usar o motor vanilla dá text/tree/table, transform (JSON Query, JMESPath, JSONPath, JS), schema (Ajv), jsonrepair e CSV sem reimplementar.

Diff estrutural: implementação própria pequena e testável; o site de referência destaca diffs no modo tree — no MVP a barra de status mostra contagens e listas de caminhos.

Cloud/login: fora (constituição).
