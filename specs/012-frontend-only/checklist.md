# Checklist — 012-frontend-only

- [x] Build `npm run build` sem variáveis de API
- [x] Testes Vitest passam sem `dotnet run`
- [x] Editor + toolbox abrem com rede offline (após 1º load)
- [x] `/privacidade` encerra sessão local e limpa workspace
- [x] Nenhuma request para `/api/session` ou `/health` no DevTools
- [x] Textos pt-BR não mencionam API do portal
- [x] `staticwebapp.config.json` com security headers
