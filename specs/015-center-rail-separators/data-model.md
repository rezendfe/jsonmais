# Data model — 015-center-rail-separators

Sem entidades novas. Tokens CSS usados:

| Token | Uso |
|-------|-----|
| `--border` | divisor horizontal entre grupos (light e dark) |
| `--toolbar-bg` | fundo do trilho (inalterado) |

Nenhum token novo obrigatório.
